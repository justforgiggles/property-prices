{
  "name": "@property-prices/function",
  "private": true,
  "type": "module",
  "engines": { "node": "22" },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "gcp-build": "node scripts/check-models.mjs && npm run build",
    "prepare:models": "node scripts/prepare-models.mjs",
    "serve": "npm run build && functions-framework --target=predict",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^5.0.5",
    "ajv": "^8.20.0",
    "handlebars": "^4.7.9",
    "onnxruntime-node": "^1.26.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
